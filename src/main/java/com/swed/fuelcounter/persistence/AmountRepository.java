package com.swed.fuelcounter.persistence;

//@Repository
//public interface AmountRepository extends JpaRepository<Amount, Long> {

    //List<Amount> sumAmountByDate();

    //@Query(value = "SELECT Sum(price*volume) as amount, date FROM fuel_record WHERE driver_id = :driverId GROUP BY date", nativeQuery = true)
    //List<AmountDTO> sumAmountByMonthAndDriver(@Param("driverId") int driverId);


//}
